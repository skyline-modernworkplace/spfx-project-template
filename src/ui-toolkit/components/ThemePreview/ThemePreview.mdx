export const metadata = {
  parent: "theming",
};

# ThemePreview

```javascript
import { ThemePreview } from "ui-toolkit";
```

Shows preview color swatches for all the colors of the current Theme.

```jsx live=true bordered=true
<ThemePreview />
```
